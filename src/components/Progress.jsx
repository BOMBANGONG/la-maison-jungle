import React from "react";
import UiProgress from "@ied/ui-progress";

const Progress = (props) => {
  return <UiProgress color="yellow-500" spinner size="xlarge" {...props} />;
};

export default Progress;
